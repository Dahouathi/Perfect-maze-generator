first step:
initialisation SDL_Init(flags)
create window
SDL_CreaWindow(title, positionx,y,largeur,hauteur,flags)
                                                        ==> SDL_CreateWindowAndRenderer()
SDL_CreateRenderer(window,-1, x)
x ={
SDL_RENDERER_SOFTWARE
SDL_RENDERER_ACCELERATED
SDL_RENDERER_PRESENTVSYNC
SDL_RENDERER_TARGETTEXTURE
}
/////////////////////////////////////////
//initialisation draw circle
int x = 100;
int y = 100;
int R = 10 ;
int h ;

//
y+=R;
SDL_RenderDrawPoint(renderer, x , y);
h=1-R;
while(x<y)
{
  if (h>0)
  {
    y--;
    h=h + 5 +2*(x-y);
  }
  h=h+2*x+3;
  x++;
  SDL_RenderDrawPoint(renderer, x , y);
/////////////////////////////////////////
SDL_RenderDrawLine/Point/Rect(renderer,pos,)
SDL_RenderFillRect(renderer,rectangle)


creer une surface et texture
SDL_Surface
SDL_Texture

surface=SDL_LoadBMP(surface,image)

texture =SDL_CreateTextureFromSurface(renderer,surface)

//free memoir mel surface by using SDL_FreeSurface(surface)

t5ali texture fi rectangle: SDL_QueryTexture(texture,Null, null , widht mte3 rect, high du rect)
SDL_RenderCopy(render,texture,NULL,&rect):affichage du texture
